import type { ExcalidrawElementSkeleton } from '@excalidraw/excalidraw/data/transform'

const elements: ExcalidrawElementSkeleton[] = []

export default {
  elements,
  appState: { currentItemFontFamily: 5 },
  scrollToContent: true,
  isGridModeEnabled: true,
  libraryItems: [],
}
